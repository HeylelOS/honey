config_header = configure_file(configuration : configuration, input : 'config.h.in', output : 'config.h')
interface_header = files('hny.h')

headers = include_directories('.')

install_headers(interface_header)
